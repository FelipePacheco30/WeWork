services:
  - type: web
    name: wework-backend
    env: docker
    rootDir: backend
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: APP_ENV
        value: production
      - key: ALLOWED_ORIGINS
        value: https://wework-frontend.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: wework-db
          property: connectionString

  - type: web
    name: wework-frontend
    env: docker
    rootDir: frontend
    plan: free
    autoDeploy: true
    envVars:
      - key: VITE_API_BASE_URL
        value: https://wework-backend.onrender.com

databases:
  - name: wework-db
    plan: free
